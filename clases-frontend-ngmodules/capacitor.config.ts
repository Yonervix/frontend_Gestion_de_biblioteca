import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'clases-frontend-ngmodules',
  webDir: 'www'
};

export default config;
